#ifndef RTRTCLOCKCONFIG_H_
#define RTRTCLOCKCONFIG_H_

#include "tasks/DisplayTask.h"

#include "pico/cyw43_arch.h"

#include <cstdint>
#include <string_view>

namespace RTRTClock::Config {

inline constexpr std::string_view WIFI_SSID{"@CONFIG_WIFI_SSID@"};
inline constexpr std::string_view WIFI_PASSWORD{"@CONFIG_WIFI_PASSWORD@"};
inline constexpr uint32_t WIFI_AUTH_METHOD{CYW43_AUTH_WPA2_AES_PSK};

inline constexpr std::string_view NTP_SERVER{"pool.ntp.org"};
inline constexpr uint32_t NTP_UPDATE_INTERVAL{60 * 60 * 1000};

// c.f. https://github.com/nekevss/TZif-POSIX-TZ-strings
inline constexpr std::string_view RTC_TZ{"CET-1CEST,M3.5.0,M10.5.0/3"};

inline constexpr Tasks::DisplayTask::Config DISPLAY_CONFIG = {
    .i2c_block = i2c1,
    .i2c_address = 0x3C,
    .i2c_baudrate_hz = 1'000'000,
    .i2c_sda_pin = 2,
    .i2c_scl_pin = 3,

    .rotation = Tasks::DisplayTask::Config::Rotation::DEG_0,
    .contrast = 1,
};

} // namespace RTRTClock::Config

#endif // RTRTCLOCKCONFIG_H_